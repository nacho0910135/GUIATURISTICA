import MaterialCommunityIcons from '@expo/vector-icons/MaterialCommunityIcons';
import { useRouter } from 'expo-router';
import { useState } from 'react';
import { Linking, Pressable, ScrollView, Text, View } from 'react-native';
import { ThemedAlert as Alert } from '@/components/themed-alert';
import { deleteMyAccount } from '@/lib/account';
import { getMySubscriptions, hasEntitledStatus } from '@/lib/billing';
import { useApp } from '@/providers/app-provider';

import { FrogLoader } from '@/components/frog-loader';
export default function DeleteAccountScreen() {
  const router = useRouter();
  const { language, session, requireAuth } = useApp();
  const [busy, setBusy] = useState(false);
  const text = (es: string, en: string) => language === 'es' ? es : en;
  const performRemoval = async (acknowledgeActiveGooglePlaySubscriptions: boolean) => { setBusy(true); try { await deleteMyAccount(acknowledgeActiveGooglePlaySubscriptions); router.replace('/'); } catch (error) { Alert.alert('Descubriendo CR', error instanceof Error ? error.message : text('No se pudo eliminar la cuenta.', 'The account could not be deleted.')); } finally { setBusy(false); } };
  const remove = async () => {
    const activeGooglePlay = (await getMySubscriptions()).some((subscription) => subscription.provider === 'google_play' && subscription.offer_id !== 'visitor_pass_30d' && hasEntitledStatus(subscription) && (!subscription.current_period_end || new Date(subscription.current_period_end).getTime() > Date.now()));
    Alert.alert(text('Eliminar cuenta definitivamente', 'Permanently delete account'), activeGooglePlay
      ? text('Tenés una suscripción de Google Play activa. Eliminar tu cuenta no la cancela: administrala en Google Play para evitar futuros cobros. Si continuás, perderás el acceso a sus beneficios.', 'You have an active Google Play subscription. Deleting your account does not cancel it: manage it in Google Play to avoid future charges. If you continue, you will lose access to its benefits.')
      : text('Se eliminarán tu cuenta, perfil, mensajes, publicaciones, fotos, reseñas y demás datos asociados. Esta acción no se puede deshacer.', 'Your account, profile, messages, posts, photos, reviews, and associated data will be deleted. This cannot be undone.'), [
      { text: text('Cancelar', 'Cancel') },
      ...(activeGooglePlay ? [{ text: text('Administrar en Google Play', 'Manage in Google Play'), onPress: () => void Linking.openURL('https://play.google.com/store/account/subscriptions') }] : []),
      { text: text('Eliminar definitivamente', 'Delete permanently'), style: 'destructive' as const, onPress: () => void performRemoval(activeGooglePlay) },
    ]);
  };
  return <ScrollView className="flex-1 bg-ui-background dark:bg-ui-dark-background" contentContainerStyle={{ padding: 20, paddingBottom: 48 }}><View className="mx-auto w-full max-w-2xl"><Pressable accessibilityLabel={text('Volver', 'Back')} className="mb-6 h-11 w-11 items-center justify-center rounded-full bg-ui-surface dark:bg-ui-dark-surface" onPress={() => router.back()}><MaterialCommunityIcons name="arrow-left" size={23} color="#0B6B4F" /></Pressable><Text className="text-3xl font-black text-ui-text dark:text-ui-dark-text">{text('Eliminar cuenta y datos', 'Delete account and data')}</Text><Text className="mt-4 leading-6 text-ui-text-muted dark:text-ui-dark-text-muted">{text('Esta página es el mecanismo oficial de Descubriendo CR para solicitar la eliminación de una cuenta. Al confirmar, la eliminación se procesa de inmediato.', 'This is Descubriendo CR’s official account-deletion page. Once confirmed, deletion is processed immediately.')}</Text><View className="mt-6 rounded-2xl border border-red-200 bg-red-50 p-5 dark:border-red-900 dark:bg-red-950"><Text className="font-black text-red-700 dark:text-red-300">{text('Se eliminarán:', 'We will delete:')}</Text><Text className="mt-2 leading-6 text-red-700 dark:text-red-300">{text('Perfil, credenciales, publicaciones, mensajes, fotos, reseñas, avistamientos, favoritos, seguimientos y aportes creados por la cuenta.', 'Profile, credentials, posts, messages, photos, reviews, sightings, favorites, follows, and submissions created by the account.')}</Text></View>{session ? <Pressable accessibilityRole="button" className="mt-6 items-center rounded-2xl bg-red-600 p-4 disabled:opacity-50" disabled={busy} onPress={remove}>{busy ? <FrogLoader color="white" /> : <Text className="font-black text-white">{text('Eliminar mi cuenta definitivamente', 'Permanently delete my account')}</Text>}</Pressable> : <><Pressable accessibilityRole="button" className="mt-6 items-center rounded-2xl bg-ui-primary p-4" onPress={() => requireAuth(text('eliminar tu cuenta', 'delete your account'))}><Text className="font-black text-white">{text('Iniciar sesión para continuar', 'Sign in to continue')}</Text></Pressable><Pressable accessibilityRole="link" className="mt-3 items-center rounded-2xl border border-ui-border p-4 dark:border-ui-dark-border" onPress={() => void Linking.openURL('mailto:jose17mp3@gmail.com?subject=Eliminar%20cuenta%20de%20Descubriendo%20CR')}><Text className="font-black text-ui-primary dark:text-ui-dark-primary">{text('Solicitar eliminación por correo', 'Request deletion by email')}</Text></Pressable><Text className="mt-3 text-sm leading-5 text-ui-text-muted dark:text-ui-dark-text-muted">{text('Escribí desde el correo asociado. Solo pediremos lo necesario para verificar la cuenta; cierta información podrá conservarse cuando exista una obligación legal o una necesidad de seguridad.', 'Write from the associated email. We will request only what is needed to verify the account; limited information may be retained for legal or security reasons.')}</Text></>}</View></ScrollView>;
}
