import type { ComponentProps, PropsWithChildren, ReactNode } from 'react';
import { Pressable, Text, View } from 'react-native';

export function AppCard({ children, className = '' }: PropsWithChildren<{ className?: string }>) { return <View className={`rounded-card border border-ui-border bg-ui-surface p-4 shadow-sm dark:border-ui-dark-border dark:bg-ui-dark-surface ${className}`}>{children}</View>; }
export function PrimaryButton({ children, className = '', ...props }: PropsWithChildren<ComponentProps<typeof Pressable> & { className?: string }>) { return <Pressable accessibilityRole="button" className={`min-h-12 items-center justify-center rounded-control bg-ui-primary px-5 active:bg-ui-primary-pressed disabled:opacity-45 dark:bg-ui-dark-primary ${className}`} {...props}><Text className="text-base font-bold text-white dark:text-ui-dark-background">{children}</Text></Pressable>; }
export function CategoryChip({ children, active = false, icon }: PropsWithChildren<{ active?: boolean; icon?: ReactNode }>) { return <View className={active ? 'min-h-8 flex-row items-center rounded-full bg-ui-primary px-3' : 'min-h-8 flex-row items-center rounded-full bg-ui-primary-soft px-3 dark:bg-ui-dark-primary-soft'}>{icon}<Text className={active ? 'text-sm font-semibold text-white' : 'text-sm font-semibold text-ui-primary dark:text-ui-dark-primary'}>{children}</Text></View>; }
export function Screen({ children, className = '' }: PropsWithChildren<{ className?: string }>) { return <View className={`flex-1 bg-ui-background dark:bg-ui-dark-background ${className}`}>{children}</View>; }
